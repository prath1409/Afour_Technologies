// ======== Exercise 1.3 ========
  // Instructions:
  // • Inspect the error, then fix it.

  let isMark: boolean;
  isMark=true;

  console.log('[Exercise 1.3]', `${isMark ? 'Oh, hi Mark' : 'Who are you?'}`);
